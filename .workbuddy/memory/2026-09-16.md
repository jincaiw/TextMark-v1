# 2026-09-16

## 桌面对标评审（等待确认，未实施）
- 通过GitHub核实参照 pluk-inc/markdown-preview 最新正式版v0.0.58（8db1b2a），TextMark最新正式版v0.9.7（a835794）；本地与远程main仍为0.8.0（9d00871），0.9.7位于fix/export-temp-path-v0.8.1分支。确认后应从0.9.7建立优化基线，先查分叉，不直接重置main。
- 已输出 outputs/desktop-review-20260916/TextMark-桌面对比与优化方案.md 与同名HTML：42项对比、15任务B00–B14、4批次。用户要求先确认方案再实施，未改业务代码/分支/依赖。
- 最新版已含LLM交接、主题、自动保存、图片粘贴/改名、公式TeX复制、macOS原生分享与CLI/deep link，不应重复规划新增。
- 源码确认优先缺口：dirty同标签导航清状态；原生关闭仅beforeunload；全局单槽watcher和菜单广播；export删除search-match节点丢正文；编辑态卸载预览影响导出；Quick Look加载后仍抢firstResponder。尚未原生实测，不以静态分析冒充复现。
- UI建议：保留Tauri/阅读优先/单区域切换，先可靠性，再可调侧栏、折叠大纲和编辑定位、统一查找/替换、源位置切换、排版入口；即时预览增强及PDF/草稿恢复后置。历史README截图不是最新实拍，不做未经标定的像素结论。

## 桌面对标实施（2026-09-16）
- 在优化/v0.9.7-parity分支继续收口P0：菜单事件改为发往触发菜单的窗口；watcher状态按窗口label隔离，并在WindowEvent::Destroyed清理；脏文档导航/前进后退已有离开确认；原生关闭请求增加脏文档拦截；HTML/PNG/PDF导出与打印在编辑态先切到预览，完成后恢复编辑态。
- 导出克隆改为解包mark.search-match保留正文；Quick Look不再加载后抢Finder first responder；侧栏和Inspector新增可拖动/键盘调宽组件与持久化宽度。
- 已通过git diff --check；尚未完成TypeScript/Vitest/Rust编译/E2E，原因是npm Arborist安装失败且环境无cargo/rustc。菜单事件、关闭保护和编辑态导出仍需真实Tauri桌面验证；未提交、未推送。
- 继续实施P1：Sidebar大纲增加真实折叠状态、展开/折叠按钮和可见项派生过滤；App.css补充outline-row/disclosure布局。仍需编译检查ARIA与CSS布局。
- P1继续：FindBar增加替换文本、替换当前和全部替换按钮；EditorPaneHandle增加find/replace接口，编辑器支持大小写敏感的当前/全部替换；补充中英文i18n和组件测试参数。尚未运行TS/Vitest。
- 大纲增加源行号：OutlineItem新增line；heading token.map在math归一化后会漂移，改用anchorOutlineToSource按标题文本回锚原始源码行（含Setext）。编辑态点大纲跳到源码行；DocumentTabs支持左右/Home/End键盘切换；文件树补aria-expanded/aria-level。
- 环境突破：npm install --legacy-peer-deps 成功（此前edgesOut失败两次）；已跑通 tsc --noEmit、vitest 292 passed、npm run build、eslint 全绿。安装副作用会改package-lock.json（删libc字段），已git checkout还原。Rust侧仍无cargo/rustc，未编译验证。
- 新增 src/lib/readingPosition.ts（anchorForOffset/lineForAnchor/offsetForLine + 单测）：预览→编辑用标题锚点换算源码行，编辑态优先用initialLine定位；编辑→预览取编辑器顶行（lineBlockAtHeight）在下一帧换算preview scrollTop。无标题时退回滚动比例。
- 本轮验证：tsc/vitest 295 passed/lint/build 全绿；vite dev（--host 127.0.0.1）可起，HTTP 200，已停。未做浏览器视觉校验（按网页策略不做额外视觉校验，且无agent-browser）。
- 关闭保护改自绘对话框：新增src/components/UnsavedCloseDialog.tsx（存储并关闭/放弃/取消），App用documentsRef让onCloseRequested订阅依赖稳定；无路径文档不能自动保存，按钮置灰。i18n新增5键，组件测试2条。
- 工具链打通：cargo 在 ~/.cargo/bin（需显式 PATH）。cargo fmt --check / check --all-targets / test（14 passed）全过；tauri build --debug --no-bundle 成功（47,459,992 字节，sha256 bb11c301…）。常规打包因 bundle.macOS.files 指向未构建的 TextMarkQuickLook.appex 失败，且该字段被 platform-packaging.test.ts 断言，故不动配置、改 --no-bundle。
- 实机验证：新二进制经最小 .app 包装器 codesign - 后 open -n 启动，lsappinfo 显示 Foreground + WebKit XPC（webview 已起）；--thumbnail 输出 512x512 PNG。⚠️ screencapture 本轮退出码 0 但只拍到壁纸（屏幕录制权限未授予），窗口截图拿不到；⚠️ 裸 cp 复制二进制进包装器后与源 cmp DIFFERENT（小 275KB），必须 /bin/cp + cmp/shasum 校验，已写入 env-quirks skill。
- 浏览器实测（agent-browser + vite dev 5199）暴露两个真缺陷并修复：① 编辑态查找计数恒「未找到」（计数只由 PreviewPane 上报，编辑态它已卸载；editorRef.find 从未被调用）→ 导航按钮被禁用；② 「替换」用 indexOf(query, selection.to) 从光标后查找，光标在匹配之后就返回 -1 静默 return 0。修法：新增 src/lib/search.ts 共享匹配核心（buildSearchPattern/searchMatchOffsets/orderedMatchesFrom/selectionMatches/replaceAllMatches），EditorPane 接收查找状态并上报计数+定位第 N 个匹配（只改选区不抢焦点），replace 改为按偏移量取光标处或之后首个匹配并环绕回文首，PreviewPane 复用同一 pattern。实测：计数由「未找到」→「第 1 项，共 3 项」；下一处 → 「第 2 项」且编辑器渲染出选区；文末点替换 → TextMark 2→1、提示「已替换 1 项」；全部替换 → 提示「已替换 2 项」、TextMark 归零。
- 新增测试：src/lib/search.test.ts 13 项；components.test.tsx 补关闭对话框交互用例（点三按钮断言回调、无路径时「存储并关闭」置灰）。门禁：tsc / vitest 36 文件 309 项 / eslint / prettier --check 全绿（prettier 修了 App.tsx、components.test.tsx、export.ts、search.ts 四处格式）。
- 交付文档已重写 outputs/desktop-review-20260916/TextMark-对标优化实施结果.md：补 Rust 编译/打包/实机/浏览器四类证据、本轮两缺陷复现与修复、工具链说明。未验证项如实标注：多窗口菜单定向、watcher 运行时清理、Quick Look 焦点（appex 未构建）仅有编译期/源码级证据。未提交、未推送。
