# 2026-09-20

- 用户要求继续推进桌面 UI，并与 `pluk-inc/markdown-preview` 完全一致。本轮先按截图对齐方法核对参照源码与 TextMark 当前 CSS，避免目测修改。
- 参照 main 源码确认：窗口 setup 约 1100x720；左侧栏 230–400px；中心最小 420px；Inspector 270–500px、默认折叠；阅读页面 900px、正文列 820px、左右内边距 40px、15px/1.5 typography。
- TextMark 原有左右栏默认/范围已匹配，但中心列此前为 `minmax(0,1fr)`，阅读/编辑页面外框此前误用 820px。本轮修复：中心列统一 `minmax(420px,1fr)`；新增 `pageColumnWidth: 900` 与 `--document-page-width`；编辑态、阅读态和 preview host 使用 900px 页面宽度，保留 820px 内容列和 40px gutters；styleContract 增加 900px 契约。
- 验证：TypeScript 通过；`styleContract.test.ts` 与 `markdown.syntax.test.ts` 共 8 项通过；Prettier 通过。已更新实施账本；当前仍未完成原生 titlebar/toolbar 像素级同屏截图校准，工作树未提交。
- 继续复核 Toolbar 与窄窗口规则：参照默认 toolbar 顺序已保留为 flexible space、sidebar、navigation、flexible space、open actions、space、zoom、document actions、search；修正 `max-width:700px` 时中心区不应继续强制 420px，改为所有面板组合单列 `minmax(0,1fr)`，避免小屏横向溢出。契约测试 11 项通过。
- 继续推进 P0 文档口径：将实施方案中的中央区域描述同步为桌面 `minmax(420px,1fr)`、窄窗口单列 `minmax(0,1fr)`，避免方案文字与实际 CSS 冲突。
- 继续对齐 Toolbar 密度：依据参照源码将 TextMark 普通按钮、导航、侧栏、分组和搜索框高度统一到 26px，普通图标按钮宽度从 36px 收敛到 26px；文档操作组继续保留边框分组，搜索框维持 160–240px 弹性宽度。TypeScript、styleContract 6 项和组件测试 15 项通过，完整门禁待复跑。
- Toolbar 密度调整后的完整门禁通过：TypeScript、Vitest 43 文件/360 项、ESLint、format:check、git diff --check 全部通过。
- 继续对齐标签页与面板标题区：标签栏顶部 padding 5→4px、关闭按钮 25→24px；Sidebar 标题区 40→38px；Inspector 顶部 52→48px、内容起始 padding 14→12px；Outline 行高保持 30px。TypeScript、组件测试 15 项和 styleContract 6 项通过，完整门禁待复跑。
- 标签页与面板标题区调整后的完整门禁通过：TypeScript、Vitest 43 文件/360 项、ESLint、format:check、git diff --check 全部通过。
- 新增 `e2e/layout-geometry.spec.mjs` 与 wdio 路由，通过真实 Tauri WebDriver 验收 Toolbar 52px、Sidebar 240px、Outline 30px、阅读页 900px 上限及 32/40/48px 页面 padding、Inspector token 270px；构建和 2 个几何场景通过。该证据属于 WebView 计算布局，不等价于原生 titlebar 像素截图。
- 继续执行 E2E：`build:e2e` 通过；layout geometry 1 spec passed；跨进程多文档恢复 1 spec passed。secondary 生命周期首次使用新 fixture 暴露了测试固定断言 `Recovery B` 的耦合，已改为读取实际 fixture 文件名和一级标题，再次 build:e2e 与 secondary-session E2E 通过。
- 实施方案新增 secondary fixture 解耦复验记录，当前不修改产品实现；工作树仍未提交、未推送、未合并。
- 运行 shell E2E 时发现设置持久化断言固定读取默认 `/tmp/textmark-v030-e2e-config`，与 `TEXTMARK_E2E_CONFIG_DIR` 隔离目录不一致导致超时；已修复 `e2e/shell.spec.mjs` 读取同一环境变量，修复后 shell 回归 22 tests passed。
- 发布前完整门禁复跑通过：Rust fmt、Clippy `-D warnings`、Rust 20 tests、TypeScript、Vitest 43/360、ESLint、format:check、生产构建、bundle budget、git diff check、npm audit（0 vulnerabilities）和 build:e2e；并修复了 secondary `CloseRequested` 事件的 Clippy `collapsible-if` 门禁错误。
- 当前仍未提交、未推送、未合并、未发布；线上 updater、跨平台 PDF、多显示器几何和原生窗口同尺寸像素对齐仍是后续证据项。
- 复验隔离 E2E：多文档恢复 1 passed、secondary 生命周期 1 passed、布局几何 2 passed；进程级 `verify-session-recovery.mjs` 在 `SIGKILL` 后报告 `B14 abnormal-exit persistence passed`，确认异常退出 session manifest 保留。
- 复核线上 updater：稳定 `v0.10.0/latest.json` 存在且版本为 0.10.0，含 18 个 platform 条目；beta manifest 存在；公钥 key id 与签名一致。新增 `e2e/updater-runtime.spec.mjs` 真实调用 Rust 检查返回 `null`，因本机安装版本已为 0.10.0，判定为“当前已是最新”而非失败；更新器契约测试 15 项通过。
- 继续收口 Quick Look：发现 `Info.plist` 固定为历史 0.3.0，已改为由 `build-quicklook.sh` 从 package.json 注入 MARKETING_VERSION/CURRENT_PROJECT_VERSION；`test-package.sh` 现改为动态读取挂载主应用版本并与扩展版本比较，避免下个版本再次硬编码。复验 BUILD SUCCEEDED、XCTest 7/0、版本 0.10.0、架构 x86_64 arm64。
- 最新收口门禁通过：TypeScript、Vitest 43/360、ESLint、format:check、Rust fmt、Clippy -D warnings、Rust 20 tests、生产构建、bundle budget、git diff check、npm audit 0 vulnerabilities、发布契约 15 项均通过；当前仍未提交、未推送、未合并、未发布。
- 最终复核已通过 GitHub Release 元数据确认：v0.10.0 为公开 stable release（draft=false、prerelease=false、36 个资产），包含 ARM64 DMG、Universal DMG、Universal updater archive 及签名；textmark-beta 为公开 prerelease 且 latest.json 存在；本机 Quick Look 版本为 0.10.0/0.10.0。该复核不等价于重新执行完整 Actions 发布流程，也不闭合跨平台 PDF、多显示器几何、Finder 拖放和原生 titlebar 像素证据。
- 继续验收：PDF capability/export 定向测试 10 项通过；异常退出探针 `B14 abnormal-exit persistence passed`；多文档标签恢复 E2E 1 passed。当前证据仍分别覆盖 manifest 保留和同进程标签恢复，尚未形成跨进程重启后 UI 多文档恢复闭环。Quick Look 本机 appex 版本仍为 0.10.0/0.10.0；当前仓库无可直接复用的原生双应用同尺寸截图夹具。
- 2026-09-21 尝试把会话恢复 E2E 扩展为两阶段重启验收：第一阶段真实写入双文档 manifest 成功；第二阶段无启动参数重启时应用进入默认 README，未恢复 `main` manifest 中的双文档标签。由此确认跨进程重启 UI 恢复仍存在真实缺陷/未闭合证据，未保留实验性 `test_crash` 或将失败测试伪装为通过；实验改动已清理，TypeScript、ESLint、format:check 通过。