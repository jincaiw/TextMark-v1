# 2026-09-18

- 根据本机 Markdown Preview v0.0.58 与 TextMark 当前能力对标结果，整理并生成《TextMark-桌面UI对标实施方案与计划.md》。
- 方案明确实施顺序：P0 桌面 UI 骨架 → P1 工作区/Inspector → P2 高频交互 → P3 Markdown 显示 → P4 GitHub 菜单入口 → P5 macOS ARM64 DMG。
- 当前已完成方案文档，并按用户“继续”进入实施：P0 调整默认工作区几何（左栏 240px、右栏 270px）和 Toolbar 间距/紧凑行为；P1 将侧栏模式切换改为明确的“大纲/文件夹”分段 tab。
- 自动门禁通过：TypeScript、Vitest 39 文件/334 项、ESLint、format:check、diff --check、前端生产构建；浏览器运行时在 1280×577 和 960×640 验证 Toolbar、侧栏几何与窄窗状态，侧栏工作区切换和 ARIA 状态通过。
- 当前未提交、未推送、未修改版本号或发布配置；工作树包含 P0/P1 UI 改动、方案文档和本日记忆变更。
- 继续实施 P1 Inspector：新增 InspectorMode（outline/document/tags/outgoing/backlinks），Inspector 顶部改为可横向滚动的面板 Tab；文稿面板保留原属性/统计，标签和出链提供当前文档基础解析，反向链接提供明确空状态，大纲可直接跳转。新增中英文 i18n 文案和面板样式。
- Inspector 改动门禁通过：TypeScript、Vitest 39 文件/334 项、ESLint、format:check、diff --check、生产构建；浏览器运行时确认 Inspector 打开、五个 Tab、标签空状态和面板 ARIA selected 状态，已保存 `/tmp/textmark-p1-inspector-tags.png`。未提交、未推送。
- 继续实施 P2 高频交互：Toolbar 增加当前文档上下文标题；标签页补 Enter/Space 激活、Delete/Backspace 关闭键盘操作；活动标签增加焦点轮廓和底部强调；窄窗口收紧文档标题和 Toolbar 布局。
- P2 门禁通过：TypeScript、Vitest 39 文件/334 项、ESLint、format:check、生产构建、diff --check；浏览器运行时在 1280×577 与 960×640 验证文档标题、侧栏 240px、搜索收缩为 36px、Toolbar 溢出菜单仍可用；截图 `/tmp/textmark-p2-wide.png`、`/tmp/textmark-p2-narrow.png`。未提交、未推送。
- 继续实施 P3 Markdown 显示细节：调整标题层级/字距/间距、段落与列表节奏、引用块强调、表格容器与表头、代码块间距与圆角、图片块级居中与上间距、行内高亮颜色；未改变 Markdown 语义或导出逻辑。
- P3 门禁通过：TypeScript、Vitest 39 文件/334 项、ESLint、format:check、diff --check、生产构建；浏览器实测正文版心 820px、正文行高 22.8px、H1 27.3px、段落间距 14px、表格表头背景生效，宽屏/窄屏截图为 `/tmp/textmark-p3-preview.png`、`/tmp/textmark-p3-narrow.png`。当前样本未含 mark/img，已如实记录；未提交、未推送。
- 方案采用自动化门禁、Tauri/浏览器运行时验证、截图与计算样式复核、阶段放行和持续监控闭环。
- P4/P5 已收口：`src/lib/menuCommands.ts` 统一 GitHub 项目主页、Releases、报告问题三条链接，新增映射测试；原生菜单契约测试确认 macOS 应用菜单与非 macOS Help 菜单均包含入口。
- 发布链路新增 `aarch64-apple-darwin` 独立 DMG 构建，规范产物为 `TextMark_<version>_arm64.dmg`，Universal DMG 为 `TextMark_<version>_universal.dmg`；两者均执行 macOS 安装/Quick Look smoke。Updater 继续使用签名 Universal `.app.tar.gz`。
- 更新 `README.md`、`INSTALL.md`、`RELEASE_NOTES.md` 的 Apple Silicon 下载说明；发布资产校验同时要求两类 DMG。
- 最终门禁通过：Vitest 40 文件/340 项、定向测试 19 项、TypeScript、ESLint、format:check、生产构建、包体预算、Rust fmt、Rust 15 项测试、release workflow YAML 解析。尚未在本机生成 ARM64 DMG，尚未完成真实原生菜单点击/浏览器跳转；工作树仍未提交。
- 本机 ARM64 构建实测：`rustup` 已安装 `aarch64-apple-darwin`，Tauri/Rust 原生 ARM64 二进制成功生成，`file`/`lipo` 均确认 `arm64`；DMG 打包在 Quick Look appex 缺失处停止。尝试运行 `platform/macos/build-quicklook.sh`，因 app-extension 配置要求 provisioning profile 而失败；未修改签名配置，不能宣称本机 DMG 已生成。
- 已启动 `/Applications/TextMark.app` 并确认安装包主二进制为 Universal（x86_64 + arm64）；macOS 自动化权限阻止通过 System Events 读取原生菜单，原生菜单点击/默认浏览器跳转仍未取得运行时证据。
- 修复本机构建生成的 `bundle_dmg.sh` 使用 BSD grep 时的正则兼容性，并以 `--skip-jenkins` 绕过 Finder AppleScript 权限限制；ARM64 DMG `TextMark_0.9.9_arm64.dmg` 已生成，包测试通过，应用为 `arm64`，Quick Look 为 `x86_64 arm64`。
- 安装 `x86_64-apple-darwin` Rust target 后，Universal 应用和 Universal DMG `TextMark_0.9.9_universal.dmg` 已生成，应用与 Quick Look 均为 `x86_64 arm64`，包测试通过。
- Universal Tauri 构建最后因本机未提供 `TAURI_SIGNING_PRIVATE_KEY` 报错，但不影响已生成的 Universal 应用包和手工打包 DMG；Updater 签名归 CI/发布环境负责，未伪造本机签名成功。
- 版本提升至 `0.10.0`，提交 `d4bf826` 已推送到 `main`，并创建/推送标签 `v0.10.0`；本地 TypeScript、Vitest 340 项、ESLint、format、Rust fmt 和 Rust 15 项测试复核通过。
- v0.10.0 GitHub Release 工作流已启动；质量门禁和 draft release 已通过，跨平台构建仍在运行，不能提前宣称正式发布完成。
- v0.10.0 Release 工作流 `35362385137` 已全部成功：质量门禁、macOS Universal/ARM64 DMG、Windows x64/ARM64、Linux x64/ARM64、Fedora x64/ARM64、资产校验、签名 updater metadata 和 SBOM 均完成；正式 Release 已于 2026-09-18 15:51 UTC 发布，GitHub 地址为 https://github.com/jincaiw/TextMark-v1/releases/tag/v0.10.0。
