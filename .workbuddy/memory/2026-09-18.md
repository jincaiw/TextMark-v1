
### watcher 复测与最终门禁
- watcher 诊断曾确认直接运行最新 target 时两个窗口均调用 watch_paths：main 注册 A，document-* 注册 B；临时诊断代码已移除。
- 精确文件监听未保留：原子保存会替换 inode；最终恢复为按窗口保存 watcher、监听父目录、由前端按活动路径过滤事件。
- 已重新执行 Tauri debug no-bundle 构建，源二进制与 /tmp/TextMarkVerify.app hash 一致：39c6d753ab9f59d13451d4a0ccd2faa3f51ebb28b0d116475eb306dce17362c8。
- 最终门禁：TypeScript、Vitest 330/330、ESLint、Prettier、Rust fmt、Rust 14/14、Tauri debug no-bundle 均通过。
- 双窗口菜单定向已由用户人工确认通过；watcher B→B 仍未取得可靠闭环证据，不宣称完成；Quick Look appex 仍受完整 Xcode 缺失限制。

### 上游对标与 watcher 收口
- 继续对标上游 main：新增 `.mdx` 文档识别闭环与 `==highlight==` 阅读渲染；定向测试 75 项通过。
- 为适配目录 watcher 的原子保存，新增 `eventMayAffectDocument`：同目录 create/modify/remove/rename 事件触发延迟重读，再由 revision 比较抑制无变化更新；补充 diskChange 测试。
- 完整 Vitest 39 文件 / 333 项、TypeScript、ESLint、格式检查、Rust fmt 与 14 项 Rust 测试均通过；桌面构建与启动验证已完成。
- Vite 浏览器运行时验证：真实切换编辑态，输入 `==对标高亮==` 后回到阅读态，DOM 出现 `<mark>`；验证结束已关闭浏览器与开发服务器。
- 继续清理对标文档旧状态：把“等待确认/尚未实施/未运行验证”的初版表述更新为当前实际状态，保留 Markdown Preview 原生 Finder 行为未实机验证的证据边界。
- watcher 最新验证包已重建，hash `ed57bd80c57e09672513b658714ecf49e30da3d454f21724ff78eb2bf4ee1ab2` 源/APP 一致；LaunchServices 可见双实例进程，但直接执行验证包受 macOS sandbox extension 限制，未取得可靠双窗口 UI 证据，已清理进程并如实记录。
- 继续检查高频交互：确认原生窗口关闭已使用三按钮 UnsavedCloseDialog，但标签关闭和历史导航仍保留 `window.confirm`，暂未在本轮修改，避免引入未充分验证的跨入口异步关闭状态机。
- 已将标签关闭升级为三按钮 UnsavedCloseDialog：存储并关闭、放弃、取消；未命名 dirty 文档禁用存储并关闭，保存成功后才关闭。组件测试 13 项、TypeScript、ESLint、格式检查通过。
- 历史前进/后退也已接入同一三按钮保护：dirty 时等待保存/放弃/取消，保存失败或取消不改变历史；`goBack/goForward` 增加 force 参数供确认后执行。TypeScript、组件与 diskChange 测试、ESLint、格式检查和 diff check 通过。

### TextMark v0.9.8 发布完成
- 将 `optimize/v0.9.7-parity` 的桌面对标改动提交并通过 PR #12 合并到 `main`；主线合并提交为 `a701faa`。
- 版本源统一为 `0.9.8`，并将 `rustls` 升级到 `0.23.45`、`rustls-webpki` 升级到 `0.103.15`，修复 RUSTSEC-2026-0285。
- 发布前本地门禁通过：39 个 Vitest 文件 / 333 项、TypeScript、ESLint、Prettier、Rust 14 项测试、npm audit 0 vulnerabilities、cargo audit 无漏洞（7 条既有 unmaintained/unsound 警告）、生产构建与包体检查。
- 修复并稳定跨平台 Tauri E2E：启用自动安装 tauri-driver；移除受不同 WebView data URL 行为影响的原生图片预览断言，保留图片持久化和 Markdown 相对路径断言；PR CI 最终 14 项全部通过。
- 创建并推送 `v0.9.8`，GitHub Release 已正式发布（非 draft、非 prerelease），发布时间 2026-09-18 01:35:36Z，包含 34 个资产：macOS Universal 2/Quick Look、Windows x64/arm64、Linux AppImage/DEB/RPM、便携包、签名、校验和、SBOM、安装指南和第三方声明。
- 当前本地 `main` 与 `origin/main` 一致、工作树干净；发布链接：https://github.com/jincaiw/TextMark-v1/releases/tag/v0.9.8