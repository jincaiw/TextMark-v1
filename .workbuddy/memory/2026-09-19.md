# 2026-09-19

- 继续闭合 B14 会话状态恢复：新增 `SourcePosition`、`EditorSelectionState`、`EditorSessionState`，以可选字段挂入 `DocumentSession` 与 `DraftRecord`，保持旧草稿记录兼容。
- `EditorPane` 新增 `getState()` 和 `onStateChange`，记录 anchor/head 选区、顶部源码行和滚动比例；返回编辑器时支持恢复完整选区，仍遵循阅读位置变化后不强行恢复旧选区的规则。
- `useDocument` 新增 `reportEditorState`，草稿恢复保留 editorState；`App.tsx` 在模式切换和标签激活时接入状态恢复。
- 新增草稿选区持久化、旧记录兼容测试；全量 Vitest 42 文件/354 项、TypeScript、ESLint、format:check、git diff --check 全部通过。工作树仍未提交、未推送。
- 当前仍未完成完整跨进程标签页清单/活动标签恢复、异常退出实机验证，以及 Windows/Linux PDF 和 B11/B12 的真实运行验收。
- 继续实现跨进程会话清单第一阶段：Rust 新增 `session-v1.json` 原子读写命令与按窗口快照结构，前端新增加载/保存桥接；主窗口无显式启动路径时恢复上次文档路径列表、活动 tab 和 workspace，运行期间 debounce 保存，关闭前 flush。Rust 15 项测试、前端 Vitest 42 文件/354 项、TypeScript、ESLint、format:check、diff --check 均通过。
- 当前会话清单只覆盖 `windowId=main`，尚未恢复 secondary windows、single-instance pending queue、窗口尺寸位置；工作树仍未提交、未推送。
- 继续扩展多窗口基础：前端通过 Tauri 当前窗口 label 生成 `windowId`，secondary 文档窗口可按自身清单恢复并按自身 ID 保存；关闭流程统一先保存当前窗口快照再关闭，clean 窗口也会执行 flush。全量前端 42 文件/354 项、Rust 15 项测试、TypeScript、ESLint、format:check、Rust fmt、diff --check 通过。
- 当前仍未完成 Rust 侧 secondary window 自动创建、窗口销毁后清理清单、single-instance 启动早期事件队列和真实多窗口重启验收。
- 继续补齐 secondary 恢复：Rust 新增 `open_session_window`，主窗口启动恢复时按 manifest 创建历史 secondary 窗口；新增 `remove_session_window`，无文档窗口快照会从清单移除。窗口 label 通过现有 Tauri API 传递，前端与 Rust 门禁均通过；当前仍缺真实多窗口重启验收和 single-instance pending queue。
- 继续补齐 single-instance 启动事件队列：Rust 新增 `PendingOpenPaths` 状态与 `drain_pending_open_paths` 命令；single-instance 回调先写入 native pending buffer 再 emit，前端启动恢复完成后主动 drain 并串行打开，避免 listener 注册前事件丢失。Rust 15 项、前端 42 文件/354 项、TypeScript、ESLint、format:check、diff --check 通过。
- 清理 pending queue 重复 `#[test]` 属性；将 `open-paths` 事件统一改为触发 native queue drain，并在 listener 注册晚于恢复完成时补做一次 drain，避免事件与主动 drain 双消费。Rust 测试恢复为 16 项且无 warning，前端 42 文件/354 项、TypeScript、ESLint、format:check、Rust fmt、diff --check 通过。
- 改进 secondary 会话恢复：创建窗口时把原窗口 ID 放入 `restoreWindowId` 查询参数，窗口启动后按该 ID 恢复完整文档列表，而不是只打开快照首个文档；仍缺真实多窗口重启与窗口销毁清理验收。
- 继续修正 pending queue 并发消费：在 `useDocument` 增加串行 `openPathsQueueRef`/`enqueueOpenPathDrain`，启动完成、事件到达和 listener 晚注册均通过同一 drain 队列消费，防止多次 drain 并行导致打开顺序或重复文档。全量门禁通过：TypeScript、ESLint、format:check、Rust fmt、Rust 16 项测试、Vitest 42 文件/354 项、git diff --check。
- 完成一次真实桌面 E2E 验收：首次使用既有 debug 二进制时 WebDriver 因应用提前退出无法启动；重新执行 `npm run build:e2e` 生成带 e2e feature 的二进制后，`npm run test:e2e -- --spec e2e/shell.spec.mjs` 成功，21 个 shell 场景全部通过。代码补充 secondary 恢复窗口把逻辑 `windowId` 写回 `windowIdRef`，确保后续保存覆盖原窗口快照而不是生成新 label 快照。真实多窗口重启、窗口销毁清理仍未由本轮 E2E 覆盖。
- 正常关闭清理会话快照：`saveSession(true)` 在关闭流程先移除当前逻辑窗口的 manifest 记录，再关闭窗口；异常退出仍保留此前保存的快照用于恢复。完成 TypeScript、ESLint、format:check、Rust fmt、Rust 16 项测试、Vitest 42 文件/354 项、git diff --check，以及重建后的桌面 E2E 21 项通过。
- 补齐窗口几何恢复：`SessionWindowSnapshot` 新增可选 `geometry`（x/y/width/height），前端保存当前窗口 outer position + inner size，恢复时应用几何；旧 manifest 无 geometry 时安全跳过，并增加 Rust 兼容测试。Rust 测试增至 17 项全部通过，前端门禁全部通过。一次重跑 E2E 在外部写冲突场景暴露既有测试状态污染/竞态，未将其宣称为通过；需后续单独修复测试隔离后再验收窗口几何。
- 修复桌面 E2E 外部冲突场景的失效引用：磁盘重载会卸载并重新挂载 CodeMirror，测试改为重新获取 `.cm-content` 并等待新内容；预览标题改用显式 `waitUntil`。新增窗口尺寸契约断言（1280x800），重建 e2e 包后 shell E2E 22 项全部通过。完整门禁继续通过：Vitest 42 文件/354 项、Rust 17 项、TypeScript、ESLint、format:check、git diff --check。
- 强化会话快照数据契约：新增 `normalize_session_snapshot`，保存前按路径去重、将 activeIndex 限制在有效文档范围，空文档快照不写入；增加 3 个 Rust 测试（去重/activeIndex、空快照、legacy geometry），Rust 测试增至 19 项全部通过，前端 354 项继续通过。
- 审查 B11-B15 后确认：B11/B12/B14 仍缺真实桌面运行证据，B13 不能在当前 macOS 环境宣称跨平台闭合，B15 功能代码已完成但原生 `.mdx` 冷启动证据不完整。当前环境选择优先补 B14 对话框可访问性：草稿恢复、未保存关闭和文件冲突对话框统一接入焦点进入、Tab 陷阱、Escape 取消/放弃，并新增 Escape 回归测试；修正 jsdom 中 `offsetParent` 不可用的可见性判断。完整 Vitest 42 文件/355 项、TypeScript、ESLint、format:check、Rust fmt、Rust 19 项测试、git diff --check 全部通过。工作树仍未提交、未推送；真实跨进程恢复、异常退出和多平台 PDF 仍未闭合。
- 重新构建带 `e2e` feature 的 Tauri debug 包成功；shell E2E 未能完整放行：外部磁盘冲突场景在“从磁盘重新载入”后等待 CodeMirror 内容更新超时，随后 toolbar 场景因应用状态未恢复而无法找到标题。该结果作为未闭合证据记录，不宣称桌面 E2E 通过；已将保存前置断言从脆弱的“必须出现两行 Preview”改为验证至少存在一行，并保留重载后重新查询编辑器节点的测试隔离修复。后续应单独定位冲突重载的真实 WebView 状态更新链路。
- 定位并修复外部磁盘重载后的 CodeMirror 状态竞态：编辑器组件此前没有随文档 revision 变化而重建，CodeMirror 保留旧内部文档状态；在 `App.tsx` 为 `EditorPane` 增加基于 `document.id + document.revision` 的稳定 key，使 reload 后创建新编辑器实例并消费最新 `value`。先隔离冲突场景通过，再完整 shell E2E 22 项通过。完整门禁通过：Vitest 42 文件/355 项、TypeScript、ESLint、format:check、Rust fmt、Rust 19 项、git diff --check。该修复属于 B12/B14 交界的真实桌面状态交接闭环，工作树仍未提交、未推送。
- 继续补强 B14 会话清理契约：抽取 `remove_window_from_manifest` 纯函数，确保移除一个 secondary 窗口时保留 main/其他 secondary，删除未知窗口幂等；新增 Rust 回归测试。Rust 测试增至 20 项，前端 Vitest 42 文件/355 项、TypeScript、format:check、git diff --check 通过。正常关闭清理和异常退出保留的真实跨进程证据仍需后续补齐。
- 继续补齐 B14 恢复计划：新增 `src/lib/sessionRestore.ts` 与 5 项测试，按实际可读文档重新计算 activePath，失效文档不再导致活动标签指向错误路径，并以稳定逻辑 windowId 选择恢复窗口；`useDocument.ts` 接入该纯逻辑。完整前端测试增至 43 个文件/360 项，Rust 20 项，TypeScript、ESLint、format:check、Rust fmt、git diff --check 全部通过；重建 E2E 包后 shell E2E 22 项全部通过。仍未取得真实异常退出重启恢复证据。
- 本轮复核并更新 `outputs/desktop-review-20260918/TextMark-桌面UI对标实施方案与计划.md`：将文档从“待确认”改为“实施中”，补入 P0–P5、B11–B14 当前账本及 B14-M1 异常退出恢复验收计划；未执行提交、推送、合并、发布。全量门禁再次通过：43 个 Vitest 文件/360 项、Rust 20 项、TypeScript、ESLint、format:check、Rust fmt、git diff--check。
- 新增 `scripts/verify-session-recovery.mjs`，以隔离 `TEXTMARK_E2E_CONFIG_DIR` 启动真实 Tauri debug 进程，验证 `session-v1.json` 原子快照在 SIGKILL 后重启仍保留；修正 Rust 配置目录覆盖在普通 debug 构建中不可用的问题，并处理 macOS `/var` 与 `/private/var` 路径规范化。该探针已通过单文档异常退出持久化验证；不宣称已完成 UI 多标签/草稿完整恢复 E2E。新增脚本已通过 Prettier、ESLint 和 `format:check`，方案文档同步记录该证据边界。
- 新增 `scripts/verify-session-cleanup.mjs` 做正常关闭进程探针；结果确认 SIGTERM 不等价于 UI 关闭流程，session manifest 仍保留，因此没有把该结果宣称为正常关闭清理通过。正常关闭清理仍需通过真实桌面关闭按钮/原生 close-request 交互验收。新增两个脚本后完整门禁通过：Vitest 43 文件/360 项、TypeScript、ESLint、format:check、Rust fmt、Rust 20 项、git diff --check。
- 新增独立 `e2e/session-cleanup.spec.mjs` 与 wdio cleanup fixture 路由；重建 e2e 包后真实 WebDriver 点击 `.traffic-lights` 关闭按钮，场景通过，并在保留配置目录模式下确认 `session-v1.json` 已被移除，B14 正常关闭清理取得 UI E2E 证据。一次外层 zsh 变量名 `status` 冲突仅影响包装命令退出码，不影响 WebDriver 场景本身；后续已用 `result` 重跑并确认清理通过。
- 补齐 B14 多文档标签验收：wdio 支持恢复专用 fixture 与隔离设置，Rust e2e 命令 `test_open_path` 通过 native pending queue 注入第二文档，新增 `e2e/session-recovery.spec.mjs` 验证两个真实标签、活动标签和 `session-v1.json` 的文档顺序/`activeIndex=1`。重建 e2e binary 后该 E2E 通过；全量前端 43 文件/360 项、TypeScript、ESLint、format:check、Rust fmt、Rust 20 项测试和 git diff --check 通过。当前证据覆盖同一进程的多文档标签与快照写入，仍未覆盖跨进程重启后 UI 恢复、secondary 窗口和几何恢复。
- 更新 B14 实施账本，明确多文档标签 E2E 已通过，并保留跨进程重启 UI、secondary 销毁清理、多显示器几何为未闭合证据；新增后续任务，工作树仍未提交、未推送、未合并、未发布。
- 继续验证跨进程恢复：尝试将进程探针扩展为双文档启动，但由于设置加载和显式多路径启动的时序不适合作为无 UI 进程探针，未扩大其承诺范围；已恢复 `scripts/verify-session-recovery.mjs` 为稳定的单文档异常退出/重启保留验证，并确认探针通过。多文档标签证据仍由真实 WebDriver E2E 提供。
- 尝试新增 secondary 窗口真实 E2E：窗口创建和文档显示可达，但当前测试环境的 secondary 窗口未写入隔离 `session-v1.json`，等待 20 秒后失败；未将失败测试保留到工作树，也未宣称 secondary 生命周期通过。定位到新窗口原生 surface 挂载期间几何查询可能拒绝，已让 `currentWindowGeometry()` 失败时返回 null，并让 `saveSession()` 使用 `activeIdRef` 读取最新活动标签，前端门禁通过；secondary E2E 尚未重新通过。
- 完成 secondary 窗口关闭清理闭环：Toolbar 关闭按钮改为调用 App 层 `requestClose`，不再直接绕过 dirty/clean 会话保护；新增 `remove_current_window_session` 原生命令按当前 native label 删除 manifest 快照，并保留 `CloseRequested`/`Destroyed` 兜底清理。重建带 e2e feature 的二进制后，真实 `secondary-session.spec.mjs` 通过（1 spec passed），创建、文档显示、快照写入、关闭清理均已取得证据。B14 当前仍缺跨进程多文档 UI 恢复和多显示器几何实机证据。
- 补充 B11-B13 当前 macOS 可验证证据：使用 `/tmp/textmark-syntax-fixture.md` 运行真实 Markdown 语法 E2E，frontmatter、标题、表格、任务列表、脚注、数学、提示框、Mermaid、危险 HTML 过滤、换行和长代码块均可到达；但图片自然尺寸和表格对齐受 WebView/夹具细节影响，本轮不将该探针计入最终放行，保留 B11 定向单测 44/44 通过。B13 `pdfCapability` 定向测试 3 项通过，明确 macOS Tauri native-vector、浏览器 browser-vector、其他 Tauri 平台 raster-fallback 的边界；B12 既有 LLM/外部交接契约继续通过，Finder 真实拖放和外部应用启动仍未宣称完成。
- 将 B11 Markdown 语法 E2E 的不稳定断言改为稳定契约：图片验证 alt/source，而非 WebView naturalWidth；表格验证 renderer-owned `md-table-align-*` class，而非 computed style。使用 `/tmp/textmark-syntax-fixture.md` 真实 macOS Tauri E2E 通过（1 spec passed），B11-B13 定向单测继续 44/44 通过；已运行 Prettier 与 git diff --check。该 E2E 证据可用于推进 B11，但 B12 Finder/外部应用及 B13 跨平台 PDF 仍未闭合。
